import { jsPDF } from 'jspdf'
import { dateText, money, paidTotal, saleStatus } from './data.js'
import logo from './logo.png?inline'
export function makeSalePdf(sale,client,trip,settings){const doc=new jsPDF({unit:'mm',format:'a4'});doc.setFillColor(18,58,97).rect(0,0,210,42,'F');doc.addImage(logo,'PNG',14,7,32,32);doc.setTextColor(255).setFont('helvetica','bold').setFontSize(22).text('CONFIRMAÇÃO DE RESERVA',54,19);doc.setFontSize(11).setFont('helvetica','normal').text(settings.company,54,27);doc.setTextColor(18,58,97).setFontSize(12).setFont('helvetica','bold').text(`Reserva ${sale.reservation}`,16,57);doc.setFontSize(18).text(trip.title||'Excursão',16,68);doc.setFontSize(10).setFont('helvetica','normal');const lines=[['Titular',client.name],['CPF',client.cpf],['Telefone',client.phone],['Destino',trip.destination||'—'],['Embarque',dateText(trip.departureDate)],['Retorno',dateText(trip.returnDate)],['Passageiros',String(1+(sale.dependents||[]).length)],['Situação',saleStatus(sale)],['Valor total',money(sale.total)],['Valor pago',money(paidTotal(sale))],['Saldo',money(Math.max(0,Number(sale.total)-paidTotal(sale)))]];let y=82;for(const [a,b] of lines){doc.setFillColor(y%2?246:238,244,238).roundedRect(16,y-5,178,9,1,1,'F');doc.setFont('helvetica','bold').text(a,20,y);doc.setFont('helvetica','normal').text(String(b||'—'),70,y);y+=11}if(sale.dependents?.length){doc.setFont('helvetica','bold').text('Dependentes',16,y+4);doc.setFont('helvetica','normal');sale.dependents.forEach((d,i)=>doc.text(`${i+1}. ${d.name}${d.cpf?' · '+d.cpf:''}`,20,y+14+i*7))}doc.setDrawColor(241,98,58).line(16,255,194,255);doc.setFontSize(8).setTextColor(80).text('Documento de confirmação gerado pelo sistema Rota Viva Excursões. A confirmação definitiva está condicionada à quitação.',16,263,{maxWidth:178});doc.text(`${settings.owner} · CPF ${settings.ownerCpf}`,16,271);return doc}
export async function downloadPdf(doc,name){const blob=doc.output('blob'),file=new File([blob],name,{type:'application/pdf'});if(navigator.canShare?.({files:[file]})){try{await navigator.share({files:[file],title:name});return}catch(e){if(e.name==='AbortError')return}}doc.save(name)}
